<!DOCTYPE html>
<html>
  <head>
    <title>InstaClone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <script>
  $(function(){
      $(".alert").fadeOut(1500);
    });
  $(function() {
    var height=$(".header").height();
    $(".main").css("margin-top", height+10);
  });
  </script>

  <body>

    <div class="container">
      <nav class="navbar navbar-fixed-top">
        <header class="header">
        <%= link_to image_tag('logo_image.png', class: 'logo_image center-block'), stories_path, {class: "link_box center-block"} %>
      </header>
      </nav>

      <div class="row">
        <div class="main col-sm-offset-1 col-sm-10">
          <% flash.each do|key, value| %>
            <%= content_tag(:div, value, class: "alert alert-#{key}") %>
          <% end %>
          <%= yield %>
        </div>
      </div>

      <% if logged_in? %>
      <nav class="navbar navbar-fixed-bottom">
        <footer class="footer">
          <div class="flex">
            <p>
              <%= link_to stories_path do %>
                <span class="glyphicon glyphicon-home footer_icon" aria-hidden="true"></span>
                <% end %>
              </p>
              <p>
                <%= link_to new_story_path do %>
                <span class="glyphicon glyphicon-plus footer_icon" aria-hidden="true"></span>
                <% end %>
              </p>
              <p>
                <%= link_to user_path(current_user.id) do %>
                <span class="glyphicon glyphicon-user footer_icon" aria-hidden="true"></span>
                <% end %>
              </p>
            </div>
        </footer>
      <% end %>

    </div>
  </body>
</html>
